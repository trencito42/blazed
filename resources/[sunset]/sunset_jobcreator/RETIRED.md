# RETIRED — not loaded on server

Job Creator was removed from `server.cfg`.

**Preserved UI:** the fisherman-style bottom HUD (`fishingShow` / `#fishing-panel`) is kept in `sunset_ui` — see `docs/INTERACTION_HUD.md`.

Template definitions (including fisherman gather job) remain in this folder for reference only.
